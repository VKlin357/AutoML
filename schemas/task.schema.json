{
  "type": "object",
  "required": ["task_type", "target", "metric", "split"],
  "properties": {
    "task_type": {"type": "string", "enum": ["binary", "multiclass", "regression", "timeseries"]},
    "target": {"type": "string"},
    "metric": {"type": "string"},
    "split": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {"type": "string", "enum": ["random", "stratified", "time"]},
        "n_folds": {"type": "integer", "minimum": 1}
      }
    }
  }
}
