# Optional MLOps / experiment-tracking stack.
#   pip install -r requirements-mlops.txt
# The core project runs without these; src/tracking.py degrades gracefully to a
# local JSONL log when a backend package or its credentials are missing.
mlflow>=2.9          # offline-capable tracking (file:./mlruns)
wandb>=0.16          # needs WANDB_API_KEY
clearml>=1.14        # needs clearml.conf / env creds
dvc>=3.0             # data + pipeline versioning (dvc repro)
hydra-core>=1.3      # config management
omegaconf>=2.3
tensorboard>=2.15
